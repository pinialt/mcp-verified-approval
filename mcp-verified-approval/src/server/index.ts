// Phase 4b scaffold. Real content arrives in step c — the server subpath
// will expose createApprovalGate, the CredentialStore interface, and the
// in-memory implementation. Empty for now.
export {};
