// Phase 4b scaffold. Real content arrives in step b — the shared subpath
// will hold the protocol vocabulary (canonicalArgs, computeActionHash, the
// _meta keys, error reasons, schemas, types). Empty for now so the package
// builds while the demo continues to import from @mcp-sec/shared.
export {};
